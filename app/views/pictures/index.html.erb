<p id="notice"><%= notice %></p>

<h1>Destinations</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Description</th>
      <th>BckPckr</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>

        <td align="center"><%= link_to image_tag(picture.img_url, height: "150", width: "200"), picture %></td> 
        <td align="center"><%= picture.title %></td>
        <td align="center"><%= picture.user.username %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
