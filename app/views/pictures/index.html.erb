<p id="notice"><%= notice %></p>

<h1>Destinations</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Title</th>
      <th>BckPckr</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td> <img src= <%= picture.img_url %> width="167" height="117"></td>
        <td><%= picture.title %></td>
        <td><%= picture.user.username %></td>
        <td> | <%= link_to 'Pic Details', picture %> | 
        <%= link_to 'Edit Details', edit_picture_path(picture) %> | 
        <%= link_to 'Delete', picture, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Picture', new_picture_path %>
